import { Role } from "../models/Roles";

export const UserRoles = {
    ADMIN: { id= 1, name: "admin" }, as Role,
    ARTIST: { id= 2, name: "artist" } as Role,
    CUSTOMER: { id= 3, name: "customer"} as Role,
};